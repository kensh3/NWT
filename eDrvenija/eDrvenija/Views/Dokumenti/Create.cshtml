@model eDrvenija.eDrvenija.Models.dokumenti

@{
    ViewBag.Title = @Resursi.Kreiraj;
}

<h2>@Resursi.Kreiraj</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>@Resursi.Dokumenti</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.brojPreuzimanja)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.brojPreuzimanja)
            @Html.ValidationMessageFor(model => model.brojPreuzimanja)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.idOglasa, "oglasi")
        </div>
        <div class="editor-field">
            @Html.DropDownList("idOglasa", String.Empty)
            @Html.ValidationMessageFor(model => model.idOglasa)
        </div>

        <p>
            <input type="submit" value="@Resursi.Kreiraj" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink(@Resursi.NazadNaPocetnu, "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
