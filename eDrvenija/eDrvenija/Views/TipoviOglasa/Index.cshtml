@model IEnumerable<eDrvenija.eDrvenija.Models.tipovioglasa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazivTipaOglasa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazivTipaOglasa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipaOglasa }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipaOglasa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipaOglasa })
        </td>
    </tr>
}

</table>
