@model IEnumerable<eDrvenija.eDrvenija.Models.tipovioglasa>

@{
    ViewBag.Title = @Resursi.Index;
}

<h2>@Resursi.Index</h2>

<p>
    @Html.ActionLink(@Resursi.KreirajNovi, "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazivTipaOglasa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazivTipaOglasa)
        </td>
        <td>
            @Html.ActionLink(@Resursi.Uredi, "Edit", new { id = item.idTipaOglasa }) |
            @Html.ActionLink(@Resursi.Detalji, "Details", new { id = item.idTipaOglasa }) |
            @Html.ActionLink(@Resursi.Obrisi, "Delete", new { id = item.idTipaOglasa })
        </td>
    </tr>
}

</table>
